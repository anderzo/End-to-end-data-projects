# librerias en este entorno

pip install pandas --trabajar con dataframes
pip install openpyxl -- trabajar con xlsx
pip install datetime -- trabajar con fechas y horas
pip install scikit-learn -- ML maching learning
pip install numpy -- 
pip install pathlib --ya intalada por defecto para gestionar direcciones files
pip install plotly -- graficar
pip install speedtest-cli -- para la red interna
pip install matplotlib -- graficar
pip install seaborn -- graficar
pip install requests -- APIs
pip install --upgrade nbformat
pip install pydantic --para validar y gestionar datos
pip install great-expectations -- manejar ecepticiones
pip install sqlalchemy psycopg2 -- cargar SQL

pip install ipykernel --para poder usar el ipynb dentro del entorno
python -m ipykernel install --user --name=mi_proyecto --display-name "Python (mi_proyecto)"